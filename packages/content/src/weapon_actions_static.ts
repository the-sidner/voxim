// Generated — do not edit directly.
// Re-run `deno task gen-content` after adding or renaming data files.
import type { WeaponActionDef } from "./types.ts";

import bite from "../data/weapon_actions/bite.json" with { type: "json" };
import bow_shot from "../data/weapon_actions/bow_shot.json" with { type: "json" };
import crossbow_shot from "../data/weapon_actions/crossbow_shot.json" with { type: "json" };
import overhead from "../data/weapon_actions/overhead.json" with { type: "json" };
import slash from "../data/weapon_actions/slash.json" with { type: "json" };
import thrust from "../data/weapon_actions/thrust.json" with { type: "json" };
import unarmed from "../data/weapon_actions/unarmed.json" with { type: "json" };

export const weapon_actions: readonly WeaponActionDef[] = [
  bite as unknown as WeaponActionDef,
  bow_shot as unknown as WeaponActionDef,
  crossbow_shot as unknown as WeaponActionDef,
  overhead as unknown as WeaponActionDef,
  slash as unknown as WeaponActionDef,
  thrust as unknown as WeaponActionDef,
  unarmed as unknown as WeaponActionDef,
];
